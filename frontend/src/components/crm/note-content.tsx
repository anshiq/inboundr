import { RichContent } from "@/components/editor/rich-content"

/** CRM timeline note renderer: the shared rich-content component. */
export const NoteContent = RichContent

export default NoteContent
